# NEW FILE — additional dependencies ONLY for the 5 advanced features.
# Your existing backend/requirements.txt is untouched.
# Install with: pip install -r backend/advanced/requirements-advanced.txt

# --- Feature 1: Hybrid Search + Reranking ---
rank-bm25==0.2.2
sentence-transformers==3.0.1
numpy==1.26.4
cohere==5.5.8              # optional, only needed if using Cohere Rerank

# --- Feature 2: Multi-Agent Architecture ---
# Tested and confirmed working on langgraph 1.2.11 (Sept 2026). If you pin an
# older 0.x version instead, re-run test_advanced_modules.py to confirm the
# HITL resume + final_status still merge correctly.
langgraph>=1.2.0
langchain-core>=0.3.0

# --- Feature 3: Self-RAG / CRAG ---
# (uses `requests`, already in your main requirements.txt)

# --- Feature 4: Observability ---
langsmith==0.1.98                                   # optional
opentelemetry-api==1.26.0                           # optional
opentelemetry-sdk==1.26.0                           # optional
opentelemetry-exporter-otlp-proto-http==1.26.0      # optional

# --- Feature 5: Multi-Modal ---
# (uses `requests`, already in your main requirements.txt)
